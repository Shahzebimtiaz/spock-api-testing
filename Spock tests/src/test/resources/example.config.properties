# If you need to use a different endpoint than localhost, copy this file,
# rename it "config.properties", and change the "endpoint-url" to the REST API
# url that you want
endpoint-url=https://jsonplaceholder.typicode.com/
